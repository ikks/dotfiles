#!/bin/bash
grep bindsym ~/.config/sway/config 2>/dev/null | sed 's%^\s*%%;s%^bindsym %%;s%--locked %%' | grep -v -i "^# " #| grep "#" | sed 's% %#%' | gawk -F"#" '{printf "%s: %s\n" ,$3, $1}' | fuzzel -dmenu -b 000000D1 -m 00FF00 -m 00FF00 -w 50
